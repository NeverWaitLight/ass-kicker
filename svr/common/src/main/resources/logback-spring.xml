<configuration>
  <springProperty scope="context" name="APP_NAME" source="spring.application.name" defaultValue="app"/>
  <springProperty scope="context" name="LOG_LEVEL" source="logging.level.root" defaultValue="INFO"/>
  <springProperty scope="context" name="ASYNC_QUEUE_SIZE" source="logging.async.queue-size" defaultValue="1024"/>
  <springProperty scope="context" name="ASYNC_DISCARDING_THRESHOLD" source="logging.async.discarding-threshold" defaultValue="0"/>

  <property name="LOG_PATTERN"
            value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [%X{requestId:-}] %logger{36} - %msg%n"/>

  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>${LOG_PATTERN}</pattern>
    </encoder>
  </appender>

  <appender name="ASYNC_STDOUT" class="ch.qos.logback.classic.AsyncAppender">
    <queueSize>${ASYNC_QUEUE_SIZE}</queueSize>
    <discardingThreshold>${ASYNC_DISCARDING_THRESHOLD}</discardingThreshold>
    <appender-ref ref="STDOUT"/>
  </appender>

  <root level="${LOG_LEVEL}">
    <appender-ref ref="ASYNC_STDOUT"/>
  </root>
</configuration>
